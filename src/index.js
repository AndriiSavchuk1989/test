import React from "react";
import ReactDOM from "react-dom";

// element
import { InputElement } from "./InputElement";

import "./styles.css";

const rootElement = document.getElementById("root");
ReactDOM.render(<InputElement />, rootElement);
